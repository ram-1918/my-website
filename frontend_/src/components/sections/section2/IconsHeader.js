const IconsHeader = ({dark}) => (
  <div className={`${dark ? "text-black border-l-0" : "text-black border-l-4 border-l-rose-500"} text-xl font-bold px-2 small:w-full small:bg-white-100 mobile:w-full`}>
    Tech Stack
  </div>
);

export default IconsHeader