import { gradientStyle } from "../../Base/Header";

const NameAsTitle = () => {
  return (
    <div className={`${gradientStyle} font-russo`}>
      <span className={`text-xl font-light`}>Ram Chandra</span>
    </div>
  );
};

export default NameAsTitle;
